import { expect } from "chai";
import "mocha";
import { Simulator } from "../../../src/monte-carlo-search-tree/simulator";

/*
describe("Simulator", () => {
    it("should be instantiable", () => {
        const simulator = new Simulator();
        expect(simulator).to.be.instanceOf(Simulator);
    });
    describe("deploy", () => {
        it("should deploy a list of possible event", () => {
            const simulator = new Simulator();
            // const state = {}
            // simulator.setCurrentState();
        });
    });
    describe("evaluate", () => {
        it("should evaluate which one to simulate", () => {
            const simulator = new Simulator();
        });
    });
    describe("select", () => {
        it("should select which event to simulate", () => {
            const simulator = new Simulator();
        });
    });
    describe("simulate", () => {
        it("should simulate for n cycles", () => {
            const simulator = new Simulator();
        });
    });
});
*/
